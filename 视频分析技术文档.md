# CineView AI 视频分析功能技术文档

## 文档概述

本文档详细介绍 CineView AI 项目中"视频分析"功能的完整技术实现，包括技术栈、执行流程、核心参数、API标准等。适合技术新人从零开始深入理解该项目。

---

## 1. 技术栈总览

### 1.1 前端框架与语言
- **React 19.2.0** - 最新版 React 框架，用于构建用户界面
- **TypeScript 5.8.2** - 提供类型安全和代码提示
- **Vite 6.2.0** - 现代化构建工具，提供快速的开发服务器

### 1.2 AI 集成
- **@google/genai 1.30.0** - Google Gemini AI SDK
  - 使用模型：`gemini-2.5-flash` (视频分析)
  - 使用模型：`gemini-2.5-flash-image` (海报生成)

### 1.3 数据可视化
- **Recharts 3.4.1** - 用于色彩分析图表展示

### 1.4 浏览器 API
- **HTML5 Video API** - 视频播放和帧提取
- **Canvas API** - 图像处理和帧渲染
- **File API** - 本地文件读取
- **LocalStorage** - API Key 本地存储

---

## 2. 核心类型定义

### 2.1 应用状态 (AppState)
```typescript
export enum AppState {
  IDLE = 'IDLE',                      // 空闲状态
  EXTRACTING_FRAMES = 'EXTRACTING_FRAMES',  // 帧提取中
  ANALYZING = 'ANALYZING',            // AI 分析中
  COMPLETE = 'COMPLETE',              // 完成
  ERROR = 'ERROR',                    // 错误
}
```

### 2.2 语言类型 (Language)
```typescript
export type Language = 'en' | 'zh';  // 英文 | 中文
```

### 2.3 处理帧数据 (ProcessedFrame)
```typescript
export interface ProcessedFrame {
  timestamp: number;    // 时间戳(秒)
  data: string;         // Base64 编码的 JPEG 图像数据
}
```

### 2.4 视频元数据 (VideoMetadata)
```typescript
export interface VideoMetadata {
  width: number;        // 视频宽度(像素)
  height: number;       // 视频高度(像素)
  duration: number;     // 视频时长(秒)
}
```

### 2.5 镜头数据 (ShotData)
```typescript
export interface ShotData {
  shotNumber: number;       // 镜头编号
  startTime: string;        // 开始时间 "MM:SS"
  endTime: string;          // 结束时间 "MM:SS"
  duration: number | string; // 时长(秒)
  size: string;             // 景别: CU, MCU, MS, LS 等
  movement: string;         // 运镜: Pan, Tilt, Static 等
  description: string;      // 画面描述
  audio: string;            // 对白/人声
  sfx: string;              // 音效
  thumbnailIndex: number;   // 缩略图索引
}
```

### 2.6 分析结果 (AnalysisResult)
```typescript
export interface AnalysisResult {
  shots: ShotData[];    // 镜头列表
  summary: string;      // 视频总结
  title: string;        // 视频标题
  script?: string;      // 生成的剧本(缓存)
  poster?: string[];    // 生成的海报(Base64数组)
}
```

---

## 3. 视频帧提取模块 (frameExtractor.ts)

### 3.1 核心功能
从用户上传的视频文件中提取关键帧，转换为 Base64 图像数据供 AI 分析。

### 3.2 关键参数

| 参数名称 | 值 | 说明 |
|---------|---|------|
| `MAX_DURATION_SECONDS` | 300 | 最大视频时长限制(5分钟) |
| `TARGET_WIDTH` | 256 | 提取帧的目标宽度(像素) |
| `JPEG_QUALITY` | 0.4 | JPEG 压缩质量(0-1) |

### 3.3 动态采样策略

根据视频时长自动调整采样间隔，确保提取约 300 帧：

| 视频时长 | 采样间隔 | 采样率 | 预计帧数 |
|---------|---------|--------|---------|
| ≤ 30秒 | 0.1秒 | 10 fps | ~300 |
| ≤ 60秒 | 0.2秒 | 5 fps | ~300 |
| ≤ 150秒 | 0.5秒 | 2 fps | ~300 |
| ≤ 300秒 | 1.0秒 | 1 fps | ~300 |

### 3.4 提取流程

```typescript
function extractFramesFromVideo(
  videoFile: File,
  onProgress: (processed: number, total: number) => void
): Promise<{frames: ProcessedFrame[], metadata: VideoMetadata}>
```

**步骤详解：**

1. **创建 Video 元素**
   ```typescript
   const video = document.createElement('video');
   video.src = URL.createObjectURL(videoFile);
   video.muted = true;
   video.playsInline = true;
   ```

2. **监听元数据加载**
   - 获取视频时长、宽度、高度
   - 检查时长是否超过 300 秒限制
   - 根据时长计算采样间隔

3. **创建 Canvas 画布**
   ```typescript
   const canvas = document.createElement('canvas');
   canvas.width = 256;  // 固定宽度
   canvas.height = (height / width) * 256;  // 保持宽高比
   ```

4. **逐帧捕获**
   - 设置 `video.currentTime` = 当前采样时间点
   - 监听 `onseeked` 事件
   - 使用 Canvas 绘制当前帧
   - 转换为 Base64 JPEG (质量 0.4)
   - 存储到 `ProcessedFrame` 数组

5. **进度回调**
   ```typescript
   onProgress(frames.length, estimatedFrames);
   ```

### 3.5 错误处理

| 错误类型 | 触发条件 | 错误消息 |
|---------|---------|---------|
| `VIDEO_TOO_LONG` | 视频时长 > 300秒 | 拒绝处理并提示用户 |
| `Canvas Context Error` | 无法获取 Canvas 2D 上下文 | "Could not get canvas context" |
| `Video Error` | 视频文件损坏或格式不支持 | "Error processing video file" |

### 3.6 性能优化

- **小尺寸帧**：256px 宽度减少内存占用
- **低质量压缩**：0.4 JPEG 质量降低数据量
- **固定帧数**：约 300 帧，总大小约 3MB
- **流式处理**：边提取边回调，实时更新进度

---

## 4. Gemini AI 服务模块 (geminiService.ts)

### 4.1 API Key 管理

#### 4.1.1 优先级
```typescript
function getClient(apiKey?: string): GoogleGenAI
```

API Key 获取优先级：
1. **函数参数** (最高优先级) - 从 UI 传入
2. **LocalStorage** - `localStorage.getItem("gemini_api_key")`
3. **环境变量** - `import.meta.env.VITE_API_KEY`

#### 4.1.2 验证规则
- 必须以 `AIza` 开头
- 存储在浏览器 LocalStorage
- 不会发送到任何第三方服务器

### 4.2 视频分析 (analyzeVideoFrames)

#### 4.2.1 函数签名
```typescript
async function analyzeVideoFrames(
  frames: ProcessedFrame[],
  fileName: string,
  language: Language = 'en',
  apiKey?: string
): Promise<AnalysisResult>
```

#### 4.2.2 核心参数

| 参数 | 值 | 说明 |
|-----|---|------|
| `MAX_FRAMES` | 300 | 发送给 Gemini 的最大帧数 |
| `model` | "gemini-2.5-flash" | 使用的 AI 模型 |
| `timeout` | 600秒 (10分钟) | API 超时时间 |
| `responseMimeType` | "application/json" | 响应格式 |

#### 4.2.3 安全设置 (Safety Settings)

所有安全类别设置为 `BLOCK_NONE`（不阻止）：

```typescript
safetySettings: [
  { category: HarmCategory.HARM_CATEGORY_HARASSMENT, threshold: HarmBlockThreshold.BLOCK_NONE },
  { category: HarmCategory.HARM_CATEGORY_HATE_SPEECH, threshold: HarmBlockThreshold.BLOCK_NONE },
  { category: HarmCategory.HARM_CATEGORY_SEXUALLY_EXPLICIT, threshold: HarmBlockThreshold.BLOCK_NONE },
  { category: HarmCategory.HARM_CATEGORY_DANGEROUS_CONTENT, threshold: HarmBlockThreshold.BLOCK_NONE },
]
```

#### 4.2.4 System Instruction (中文版)

关键提示词要点：

1. **角色定位**：专业电影剪辑师和场记
2. **任务目标**：生成详细的分镜表
3. **输出语言**：完全使用简体中文
4. **镜头识别规则**：
   - ✅ 只在以下情况识别为镜头切换：
     - 明显的硬切（场景/角度/景别变化）
     - 转场效果（淡入淡出、划像）
     - 时间或空间的明显跳跃
   - ❌ 不算镜头切换的情况：
     - 同一镜头内的人物移动
     - 轻微光线变化
     - 画面内物体移动
     - 同一镜头内的缓慢推拉摇移

5. **分析字段**：
   - 开始/结束时间：根据截图时间戳确定
   - 景别：特写、近景、中景、全景、大全景、远景
   - 运镜：固定、摇镜头、推、拉、跟拍、手持、升降
   - 画面描述：简要描述内容和动作
   - 人声/对白：根据口型和语境推断
   - 音效：推断可能的音效
   - 缩略图索引：最能代表该镜头的图片索引

#### 4.2.5 响应 Schema

```typescript
responseSchema: {
  type: Type.OBJECT,
  properties: {
    title: {
      type: Type.STRING,
      description: "片段标题 (中文)"
    },
    summary: {
      type: Type.STRING,
      description: "片段总结 (中文)"
    },
    shots: {
      type: Type.ARRAY,
      items: {
        type: Type.OBJECT,
        properties: {
          shotNumber: { type: Type.INTEGER },
          startTime: { type: Type.STRING, description: "Format: MM:SS" },
          endTime: { type: Type.STRING, description: "Format: MM:SS" },
          duration: { type: Type.NUMBER, description: "Duration in seconds" },
          size: { type: Type.STRING, description: "景别 (必须使用中文)" },
          movement: { type: Type.STRING, description: "运镜 (必须使用中文)" },
          description: { type: Type.STRING, description: "画面描述 (必须使用中文)" },
          audio: { type: Type.STRING, description: "人声/对白 (必须使用中文)" },
          sfx: { type: Type.STRING, description: "音效 (必须使用中文)" },
          thumbnailIndex: { type: Type.INTEGER }
        }
      }
    }
  }
}
```

#### 4.2.6 帧下采样逻辑

如果提取的帧数 > 300：
```typescript
const framesToSend = frames.length > MAX_FRAMES
  ? frames.filter((_, i) => i % Math.floor(frames.length / MAX_FRAMES) === 0).slice(0, MAX_FRAMES)
  : frames;
```

- 使用 `floor` 取整确保间隔稳定
- `slice(0, MAX_FRAMES)` 保证严格不超过 300 帧
- 均匀采样，不会遗漏视频头尾

#### 4.2.7 请求构造

```typescript
{
  model: "gemini-2.5-flash",
  contents: {
    parts: [
      { text: systemInstruction },
      { text: "[Frame Index: 0, Timestamp: 0.00s]" },
      { inlineData: { mimeType: "image/jpeg", data: frame0Base64 } },
      { text: "[Frame Index: 1, Timestamp: 0.10s]" },
      { inlineData: { mimeType: "image/jpeg", data: frame1Base64 } },
      // ... 最多 300 组
    ]
  },
  config: { /* 安全设置和响应格式 */ }
}
```

#### 4.2.8 超时保护

```typescript
const apiCall = ai.models.generateContent({ /* config */ });
const timeoutPromise = new Promise((_, reject) =>
  setTimeout(() => reject(new Error("Gemini API timeout after 600 seconds")), 600000)
);
const response = await Promise.race([apiCall, timeoutPromise]);
```

- 10分钟超时
- 使用 `Promise.race` 机制
- 超时抛出明确错误消息

#### 4.2.9 响应解析

多层级 fallback 确保稳定性：

```typescript
// 1. 尝试 response.text() 方法
let resultText = typeof response.text === 'function' ? response.text() : response.text;

// 2. Fallback 到 candidates[0].content.parts[0].text
if (!resultText && response.candidates && response.candidates.length > 0) {
  const candidate = response.candidates[0];
  if (candidate.content && candidate.content.parts && candidate.content.parts.length > 0) {
    resultText = candidate.content.parts[0].text;
  }
}

// 3. 检查 finishReason 和 safetyRatings
if (!resultText) {
  const finishReason = response.candidates?.[0]?.finishReason;
  const safetyRatings = response.candidates?.[0]?.safetyRatings;
  throw new Error(`Gemini API blocked response. Reason: ${finishReason}`);
}

// 4. 解析 JSON
const data = JSON.parse(resultText) as AnalysisResult;
```

### 4.3 翻译功能 (translateAnalysisResult)

#### 4.3.1 函数签名
```typescript
async function translateAnalysisResult(
  data: AnalysisResult,
  targetLang: Language,
  apiKey?: string
): Promise<AnalysisResult>
```

#### 4.3.2 翻译字段

仅翻译以下字段：
- `title` - 标题
- `summary` - 总结
- `shots` 数组中的：
  - `size` - 景别
  - `movement` - 运镜
  - `description` - 画面描述
  - `audio` - 对白
  - `sfx` - 音效

不修改：时间戳、数字、结构

#### 4.3.3 翻译提示词

中文版：
```
你是一个专业的翻译助手。请将以下电影分镜表数据的 JSON 内容翻译成中文。
仅翻译以下字段的值：'summary', 'title' 以及 shots 数组中的 'size', 'movement', 'description', 'audio', 'sfx'。
不要修改任何数字、时间戳或结构。
'size' (景别) 和 'movement' (运镜) 请使用专业的电影中文术语。
```

### 4.4 剧本生成 (generateScreenplay)

#### 4.4.1 函数签名
```typescript
async function generateScreenplay(
  data: AnalysisResult,
  language: Language,
  apiKey?: string
): Promise<string>
```

#### 4.4.2 输入数据清洗

只发送必要字段减少 Token 消耗：
```typescript
const cleanShots = data.shots.map(s => ({
  id: s.shotNumber,
  time: s.startTime,
  desc: s.description,
  audio: s.audio
}));
```

#### 4.4.3 剧本格式要求

1. **场景标题 (SCENE HEADING)**：根据内容推断，如 "内景. 房间 - 白天"
2. **动作描写 (ACTION)**：将连续镜头的画面描述整合成连贯段落
3. **角色和对白 (CHARACTER & DIALOGUE)**：从 audio 字段提取

### 4.5 海报生成 (generateMoviePoster)

#### 4.5.1 函数签名
```typescript
async function generateMoviePoster(
  title: string,
  summary: string,
  shots: ShotData[],
  frames: ProcessedFrame[],
  apiKey?: string
): Promise<string[]>
```

#### 4.5.2 最佳角色帧识别

```typescript
async function findBestCharacterFrame(
  frames: ProcessedFrame[],
  apiKey?: string
): Promise<string | null>
```

- 每 4 帧采样一次 (降低成本)
- 使用 Gemini 识别最清晰的主角特写/中近景
- 返回 JSON: `{ "bestFrameIndex": number }`

#### 4.5.3 Fallback 策略

如果 AI 识别失败，使用元数据：
```typescript
const goodSizes = ['CU', 'MCU', 'Close Up', 'Close-Up', 'Medium Close Up', '特写', '近景'];
const bestShot = shots.find(s => goodSizes.some(size => s.size.includes(size)));
let heroFrameIndex = bestShot && bestShot.thumbnailIndex < frames.length
  ? bestShot.thumbnailIndex
  : Math.floor(frames.length / 2);  // 中间帧
```

#### 4.5.4 垂直裁剪 (9:16)

```typescript
const verticalReferenceBase64 = await cropToVertical9_16(referenceImageBase64);
```

- 目标比例：9:16 (竖版海报)
- 中心裁剪
- 保持高度，裁剪宽度

#### 4.5.5 海报生成配置

```typescript
{
  model: "gemini-2.5-flash-image",  // 图像生成模型
  contents: {
    parts: [
      { inlineData: { mimeType: "image/jpeg", data: verticalReferenceBase64 } },
      { text: basePrompt + variation }
    ]
  },
  config: {
    responseModalities: [Modality.IMAGE]  // 指定输出图像
  }
}
```

#### 4.5.6 多变体生成

并发生成 4 种风格：
1. "Close-up portrait with dramatic lighting." - 戏剧性特写
2. "Character in a key environment from the film." - 关键场景
3. "Minimalist design with bold typography and high contrast." - 极简高对比度
4. "Dynamic action pose composition." - 动态动作姿态

```typescript
const promises = variations.map(async (variation) => {
  const response = await ai.models.generateContent({ /* config */ });
  const part = response.candidates?.[0]?.content?.parts?.[0];
  return part?.inlineData?.data || null;  // Base64 图像
});
const results = await Promise.all(promises);
```

---

## 5. 视频工具模块 (videoUtils.ts)

### 5.1 高分辨率帧捕获 (captureHighResFrame)

#### 5.1.1 用途
用于生成高质量缩略图和导出

#### 5.1.2 参数

| 参数 | 说明 |
|-----|------|
| `videoFile` | 原始视频文件 |
| `timeInSeconds` | 捕获时间点(秒) |
| 返回值 | Base64 Data URL (JPEG, 质量 0.95) |

#### 5.1.3 关键差异

与 frameExtractor 的区别：
- **分辨率**：使用视频原始分辨率 (vs 256px)
- **质量**：JPEG 质量 0.95 (vs 0.4)
- **用途**：单帧高质量捕获 (vs 批量提取)

### 5.2 垂直裁剪 (cropToVertical9_16)

#### 5.2.1 算法

```typescript
const targetRatio = 9 / 16;  // 0.5625
let cropH = srcH;
let cropW = srcH * targetRatio;

if (cropW > srcW) {  // 源图已经是竖版
  cropW = srcW;
  cropH = srcW / targetRatio;
}

const startX = (srcW - cropW) / 2;  // 中心裁剪
const startY = (srcH - cropH) / 2;
```

#### 5.2.2 输出
- 返回裁剪后的 Base64 (不含 Data URL 前缀)
- JPEG 质量 0.9

### 5.3 拼贴图生成 (generateTiledImage)

#### 5.3.1 用途
生成色彩脚本的大型拼贴图

#### 5.3.2 参数

| 参数 | 默认值 | 说明 |
|-----|-------|------|
| `shots` | - | 镜头数组 |
| `frames` | - | 帧数组 |
| `videoMeta` | null | 视频元数据 |
| `columns` | - | 列数 |
| `showLabels` | true | 是否显示镜头编号 |

#### 5.3.3 布局算法

```typescript
const GAP = 10;  // 间距(像素)
const TOTAL_WIDTH = 3000;  // 固定宽度(像素)
const frameW = (TOTAL_WIDTH - (columns - 1) * GAP) / columns;

const metaRatio = videoMeta ? videoMeta.width / videoMeta.height : 16 / 9;
const frameH = frameW / metaRatio;

const rows = Math.ceil(shots.length / columns);
const TOTAL_HEIGHT = rows * frameH + (rows - 1) * GAP;
```

#### 5.3.4 渲染流程

1. 创建 Canvas (3000px 宽)
2. 填充深色背景 `#111827`
3. 并行加载所有镜头缩略图
4. 逐个绘制到计算好的位置
5. 可选：绘制镜头编号标签
6. 输出 JPEG Data URL (质量 0.9)

---

## 6. 色彩分析模块 (colorUtils.ts)

### 6.1 提取主色调 (extractDominantColors)

#### 6.1.1 函数签名
```typescript
async function extractDominantColors(
  frames: { data: string }[],
  maxColors: number = 6
): Promise<string[]>
```

#### 6.1.2 采样策略

从所有帧中均匀采样 10 帧：
```typescript
const sampleStep = Math.max(1, Math.floor(frames.length / 10));
const sampleFrames = frames.filter((_, i) => i % sampleStep === 0);
```

#### 6.1.3 颜色提取算法

**步骤 1：缩放图像**
```typescript
canvas.width = 50;
canvas.height = 50;
ctx.drawImage(img, 0, 0, 50, 50);
```
使用 50x50 像素降低计算量

**步骤 2：量化颜色**
```typescript
for (let i = 0; i < imageData.length; i += 4) {
  let r = imageData[i];
  let g = imageData[i + 1];
  let b = imageData[i + 2];
  
  // 量化：舍入到最近的 32
  r = Math.round(r / 32) * 32;
  g = Math.round(g / 32) * 32;
  b = Math.round(b / 32) * 32;
  
  const hex = rgbToHex(r, g, b);
  colorMap[hex].count++;
}
```

量化级别：
- 256 色 → 8 级 (256 / 32 = 8)
- 总共 8³ = 512 种可能颜色

**步骤 3：按频率排序**
```typescript
let sortedColors = Object.values(colorMap).sort((a, b) => b.count - a.count);
```

**步骤 4：去重相似颜色**
```typescript
const isTooSimilar = distinctColors.some(c => {
  const dist = Math.sqrt(
    Math.pow(c.r - color.r, 2) +
    Math.pow(c.g - color.g, 2) +
    Math.pow(c.b - color.b, 2)
  );
  return dist < 60;  // 欧氏距离阈值
});
```

#### 6.1.4 输出
返回最多 6 种不同的主色调 (Hex 格式)

---

## 7. 应用主流程 (App.tsx)

### 7.1 状态管理

#### 7.1.1 核心状态

```typescript
const [state, setState] = useState<AppState>(AppState.IDLE);
const [frames, setFrames] = useState<ProcessedFrame[]>([]);
const [videoMeta, setVideoMeta] = useState<VideoMetadata | null>(null);
const [analysis, setAnalysis] = useState<AnalysisResult | null>(null);
const [file, setFile] = useState<File | null>(null);
const [apiKey, setApiKey] = useState<string | null>(null);
const [language, setLanguage] = useState<Language>('en');
const [theme, setTheme] = useState<'light' | 'dark'>('light');
const [activeTab, setActiveTab] = useState<'list' | 'colorscript' | 'script' | 'poster'>('list');
```

### 7.2 完整处理流程

#### 7.2.1 文件选择 (handleFileSelect)

```typescript
async function handleFileSelect(selectedFile: File) {
  // 1. 验证 API Key
  if (!checkApiKey()) return;
  
  // 2. 切换到帧提取状态
  setState(AppState.EXTRACTING_FRAMES);
  setFile(selectedFile);
  
  // 3. 提取帧 (带进度回调)
  const { frames: extractedFrames, metadata } = await extractFramesFromVideo(
    selectedFile,
    (current, total) => {
      setProgress({ current, total, stage: `处理视频 (${current}/${total})` });
    }
  );
  
  setFrames(extractedFrames);
  setVideoMeta(metadata);
  
  // 4. 切换到 AI 分析状态
  setState(AppState.ANALYZING);
  setProgress({ current: 0, total: 100, stage: 'AI 分析中' });
  
  // 5. 调用 Gemini API
  const result = await analyzeVideoFrames(
    extractedFrames,
    selectedFile.name,
    language,
    apiKey || undefined
  );
  
  // 6. 完成
  setAnalysis(result);
  setState(AppState.COMPLETE);
  setActiveTab(initialTab);  // 跳转到用户选择的功能
}
```

#### 7.2.2 错误处理

```typescript
catch (err: any) {
  if (err.message === "VIDEO_TOO_LONG") {
    setError(`视频超过 5 分钟限制`);
  } else if (err.message.includes("API Key")) {
    setError("无效的 API Key");
    setShowKeyModal(true);
  } else {
    setError(err.message || "发生未知错误");
  }
  setState(AppState.ERROR);
}
```

### 7.3 自动剧本生成

```typescript
useEffect(() => {
  const generateIfNeeded = async () => {
    if (activeTab === 'script' && analysis && !analysis.script && !generatingScript && state === AppState.COMPLETE) {
      setGeneratingScript(true);
      try {
        const scriptText = await generateScreenplay(analysis, language, apiKey || undefined);
        setAnalysis(prev => prev ? { ...prev, script: scriptText } : null);
      } catch (e) {
        console.error("Script generation failed", e);
      } finally {
        setGeneratingScript(false);
      }
    }
  };
  generateIfNeeded();
}, [activeTab, analysis, generatingScript, language, state, apiKey]);
```

当用户切换到"剧本"标签时自动生成（如果未生成）

### 7.4 语言切换与翻译

```typescript
async function handleLanguageChange(newLang: Language) {
  if (newLang === language) return;
  setLanguage(newLang);
  
  if (analysis && state === AppState.COMPLETE) {
    setState(AppState.ANALYZING);
    setProgress({ current: 0, total: 0, stage: '翻译中' });
    
    try {
      const translatedAnalysis = await translateAnalysisResult(analysis, newLang, apiKey || undefined);
      if (translatedAnalysis.script) delete translatedAnalysis.script;  // 清除剧本缓存
      setAnalysis(translatedAnalysis);
      setState(AppState.COMPLETE);
    } catch (err) {
      console.error("Translation error", err);
    }
  }
}
```

---

## 8. 核心参数配置总表

### 8.1 视频处理参数

| 参数 | 位置 | 值 | 说明 |
|-----|-----|---|------|
| 最大视频时长 | frameExtractor.ts | 300秒 | 超过会拒绝处理 |
| 帧宽度 | frameExtractor.ts | 256px | 降低数据量 |
| JPEG 质量 | frameExtractor.ts | 0.4 | 低质量压缩 |
| 最大帧数 | geminiService.ts | 300 | 发送给 API 的上限 |

### 8.2 AI 模型参数

| 参数 | 值 | 说明 |
|-----|---|------|
| 分析模型 | gemini-2.5-flash | 快速多模态模型 |
| 海报生成模型 | gemini-2.5-flash-image | 图像生成模型 |
| API 超时 | 600秒 | 10分钟 |
| 安全过滤 | BLOCK_NONE | 不阻止任何内容 |
| 响应格式 | application/json | 结构化 JSON |

### 8.3 图像处理参数

| 参数 | 值 | 说明 |
|-----|---|------|
| 高分辨率帧质量 | 0.95 | 用于导出 |
| 拼贴图宽度 | 3000px | 固定宽度 |
| 拼贴图间距 | 10px | 帧之间间距 |
| 色彩分析采样 | 10帧 | 从全部帧中采样 |
| 色彩采样尺寸 | 50x50px | 降低计算量 |
| 色彩量化级别 | 32 | 256/32=8级 |
| 色彩相似度阈值 | 60 | 欧氏距离 |
| 最大主色调数 | 6 | 输出颜色数 |

### 8.4 海报生成参数

| 参数 | 值 | 说明 |
|-----|---|------|
| 角色帧采样 | 每4帧 | 降低成本 |
| 海报比例 | 9:16 | 竖版海报 |
| 裁剪质量 | 0.9 | 高质量 |
| 变体数量 | 4 | 并发生成 |

---

## 9. 最佳实践与性能优化

### 9.1 视频上传建议

| 视频时长 | 文件大小 | 分析时间 | 质量 |
|---------|---------|---------|------|
| < 30秒 | < 10MB | 30-60秒 | ⭐⭐⭐⭐⭐ 最佳 |
| 30-60秒 | < 20MB | 60-120秒 | ⭐⭐⭐⭐ 优秀 |
| 1-3分钟 | < 50MB | 2-5分钟 | ⭐⭐⭐ 良好 |
| 3-5分钟 | < 100MB | 5-10分钟 | ⭐⭐ 可接受 |
| > 5分钟 | - | - | ❌ 不支持 |

### 9.2 性能优化策略

#### 9.2.1 帧提取层面
- ✅ 固定 256px 宽度 (vs 原始分辨率)
- ✅ JPEG 质量 0.4 (vs 1.0)
- ✅ 动态采样率 (vs 固定高帧率)
- ✅ 流式处理 (vs 批量加载)

#### 9.2.2 API 调用层面
- ✅ 严格限制 300 帧 (vs 无限制)
- ✅ 结构化 JSON 输出 (vs 自由文本)
- ✅ 超时保护 (vs 无限等待)
- ✅ 多层级响应解析 (vs 单一路径)

#### 9.2.3 色彩分析层面
- ✅ 采样 10 帧 (vs 全部帧)
- ✅ 缩放到 50x50 (vs 原始尺寸)
- ✅ 量化到 8 级 (vs 256 级)
- ✅ 去重相似颜色 (vs 全部保留)

#### 9.2.4 海报生成层面
- ✅ 每 4 帧采样 (vs 全部帧)
- ✅ 并发生成 4 个变体 (vs 串行)
- ✅ AI 识别最佳帧 (vs 随机选择)

### 9.3 错误处理策略

| 错误类型 | 检测时机 | 处理方式 |
|---------|---------|---------|
| 视频过长 | 元数据加载后 | 拒绝处理 + 提示用户 |
| API Key 无效 | 首次调用时 | 弹出输入框 |
| API 超时 | 10分钟后 | 提示减少视频长度 |
| 响应为空 | 解析时 | 检查 finishReason |
| 网络错误 | 请求时 | 捕获异常并提示 |

### 9.4 用户体验优化

#### 9.4.1 进度反馈
- 帧提取：实时显示进度条 `${current}/${total}`
- AI 分析：显示"AI 分析中"动画
- 翻译：显示"翻译中"状态
- 剧本生成：显示"生成剧本中"动画

#### 9.4.2 缓存机制
- `analysis.script` 缓存生成的剧本
- `analysis.poster` 缓存生成的海报数组
- 切换语言时清除剧本缓存（需重新生成）

#### 9.4.3 本地存储
- API Key 存储到 LocalStorage
- 语言偏好可持久化
- 主题偏好可持久化

---

## 10. API 成本估算

### 10.1 Gemini 定价 (参考)

| 操作 | 输入 Token | 输出 Token | 估算成本 |
|-----|-----------|-----------|---------|
| 视频分析 (300帧) | ~30,000 | ~2,000 | $0.30 |
| 翻译 | ~2,000 | ~2,000 | $0.02 |
| 剧本生成 | ~2,000 | ~3,000 | $0.03 |
| 海报生成 (4张) | ~20,000 | ~4,000 | $0.24 |

**单次完整流程总成本**：约 $0.59 USD

### 10.2 成本优化建议

1. **减少帧数**：300 → 200 (成本 ↓ 33%)
2. **降低海报变体**：4 → 2 (成本 ↓ 50%)
3. **按需生成**：剧本和海报仅在用户查看时生成
4. **缓存机制**：避免重复调用 API

---

## 11. 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         用户界面 (React)                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 上传区域 │ │ 进度显示 │ │ 结果展示 │ │ 设置面板 │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                    应用逻辑层 (App.tsx)                       │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │
│  │ 状态管理   │  │ 事件处理   │  │ 生命周期   │            │
│  └────────────┘  └────────────┘  └────────────┘            │
└───────┬────────────────────┬────────────────────┬───────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌─────────────┐      ┌─────────────┐     ┌─────────────┐
│ 帧提取模块  │      │ AI 服务模块 │     │ 工具模块    │
│ frameExtr.. │      │ geminiSer.. │     │ videoUtils  │
│             │      │             │     │ colorUtils  │
│ ·提取帧     │      │ ·视频分析   │     │ ·高清帧     │
│ ·动态采样   │      │ ·翻译       │     │ ·拼贴图     │
│ ·压缩       │      │ ·剧本生成   │     │ ·色彩提取   │
│             │      │ ·海报生成   │     │ ·裁剪       │
└──────┬──────┘      └──────┬──────┘     └─────────────┘
       │                    │
       ▼                    ▼
┌─────────────┐      ┌─────────────┐
│ 浏览器 API  │      │ Gemini API  │
│ ·Video API  │      │ ·Flash      │
│ ·Canvas API │      │ ·Flash-Img  │
│ ·File API   │      │             │
└─────────────┘      └─────────────┘
```

---

## 12. 开发调试指南

### 12.1 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 12.2 环境变量

创建 `.env` 文件：
```env
VITE_API_KEY=AIza...your_key_here
```

### 12.3 调试日志

项目中的关键日志：

```typescript
console.log(`[Gemini API] Sending ${framesToSend.length} frames`);
console.log(`[Gemini API] Estimated payload: ~${size}MB`);
console.log('[Gemini API] Starting video analysis request...');
console.log('[Gemini API] Response received successfully');
console.error("[Gemini API] Empty response. Full response object:", response);
```

### 12.4 常见问题排查

| 问题 | 可能原因 | 解决方案 |
|-----|---------|---------|
| 白屏 | API Key 未配置 | 点击右上角钥匙图标输入 |
| 分析超时 | 视频过长/过复杂 | 缩短视频或降低分辨率 |
| 响应为空 | 安全过滤阻止 | 检查 finishReason |
| 海报生成失败 | 无清晰角色帧 | 使用 Fallback 逻辑 |

---

## 13. 扩展与定制

### 13.1 修改采样率

编辑 `frameExtractor.ts`：
```typescript
// 修改采样间隔
if (duration <= 30) {
  intervalSeconds = 0.05;  // 改为 20 fps
} else if (duration <= 60) {
  intervalSeconds = 0.1;   // 改为 10 fps
}
```

### 13.2 增加最大帧数

编辑 `geminiService.ts`：
```typescript
const MAX_FRAMES = 500;  // 从 300 增加到 500
```

⚠️ 注意：会增加 API 成本和响应时间

### 13.3 添加新语言

编辑 `types.ts`：
```typescript
export type Language = 'en' | 'zh' | 'ja' | 'ko';  // 添加日语、韩语
```

编辑 `utils/translations.ts` 添加翻译

### 13.4 自定义景别术语

编辑 `geminiService.ts` 的 systemInstruction：
```typescript
景别 (Size): ECU(大特写), CU(特写), MCU(中特写), MS(中景), LS(全景), VLS(大全景)
```

---

## 14. 总结

### 14.1 核心技术要点

1. **客户端处理**：视频不上传服务器，仅帧数据发送 API
2. **动态采样**：根据视频长度智能调整采样率
3. **严格限制**：300 帧上限确保稳定性和成本控制
4. **多层 Fallback**：确保各环节容错性
5. **结构化输出**：使用 JSON Schema 保证数据质量
6. **并发优化**：海报生成使用 Promise.all

### 14.2 适用场景

✅ 推荐场景：
- 短视频剪辑分析 (30秒-2分钟)
- 电影片段研究
- 广告分镜设计
- 教学视频拆解

❌ 不适用场景：
- 长视频 (> 5分钟)
- 实时分析
- 批量处理

### 14.3 后续迭代方向

1. **服务端版本**：支持长视频队列处理
2. **批量导出**：CSV、PDF、Final Cut Pro XML
3. **协作功能**：团队分享和评论
4. **模板库**：预设分镜术语模板
5. **音频分析**：集成语音识别

---

## 15. 参考资源

### 15.1 官方文档
- [Google Gemini API](https://ai.google.dev/docs)
- [React 19 Documentation](https://react.dev/)
- [Vite Guide](https://vitejs.dev/)
- [HTML5 Video API](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement)

### 15.2 项目仓库
- GitHub: [CineView-AI](https://github.com/)
- 在线演示: [Demo](https://cineview-ai-899608532064.us-west1.run.app/)

### 15.3 技术支持
- Email: eddyse@gmail.com
- Issues: GitHub Issues

---

**文档版本**: 1.0  
**最后更新**: 2025-11-30  
**作者**: Antigravity AI Agent  
**适用项目版本**: CineView AI v1.0.1
