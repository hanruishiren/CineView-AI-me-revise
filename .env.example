# 部署环境变量示例

# Render 部署（默认，使用根路径）
# 不需要设置 VITE_DEPLOY_TARGET 或设置为：
# VITE_DEPLOY_TARGET=render

# GitHub Pages 部署（使用子路径 /CineView-AI-me-revise/）
# VITE_DEPLOY_TARGET=github

# Google Gemini API Key（可选，可以在运行时通过 UI 输入）
# GEMINI_API_KEY=your_api_key_here
